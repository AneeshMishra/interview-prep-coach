server {
    listen 3000;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Client-side routing (React Router): let unknown paths fall through to
    # index.html instead of a 404 from nginx.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
